# Leave empty to use same-origin /api and /ws (recommended — works with Vite proxy and production reverse proxies)
VITE_API_URL=
VITE_WS_URL=

# Optional: override for direct backend (no proxy)
# VITE_API_URL=http://192.168.138.118:5000/api
# VITE_WS_URL=ws://192.168.138.118:5000/ws
